database:
  driver: "postgres"
  dbname: "test_pg_go"
  username: "mamun"
  password: "123"
  host: "postgres_db" # "" for local dev
  port: "5432"
  log_mode: true

server:
  host: "0.0.0.0"
  port: "8000"
  secret: "secret"
  allow_hosts: "localhost"
  environment: "dev" #prod
  request:
    timeout: 100

